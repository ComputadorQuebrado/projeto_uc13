<h1 class="text-center">Adicionar Produtos</h1>

<div class="container" style="max-width: 80%">
  <form action="/produtos/add" method="POST" class="mb-3 mx-auto">
    <div class="form-group">
      <label for="inputNome">Nome:</label>
      <input type="text" class="form-control" id="inputNome" name="nome" aria-describedby="emailHelp">
    </div>
    <div class="form-group">
      <label for="inputDesc">Descrição:</label>
      <textarea class="form-control" id="inputDesc" name="descricao" rows="3"></textarea>
    </div>
    <div class="form-group">
      <label for="inputPreco">Preço:</label>
      <input type="number" step="0.01" class="form-control" id="inputPreco" name="preco" aria-describedby="emailHelp">
    </div>
    <div class="form-group">
      <label for="inputEstoque">Estoque:</label>
      <input type="number" class="form-control" id="inputEstoque" name="estoque" aria-describedby="emailHelp">
    </div>
    <div class="form-group">
      <label for="inputCategoria">Categoria:</label>
      <select class="form-control" id="inputCategoria" name="categoria_id">
        {{#if categorias.length}}
          <option>Selecione uma categoria.</option>
          {{#each categorias}}
            <option value={{this.id}}>{{this.nome}}</option>
          {{/each}}
        {{else}}
          <option value="">Sem categorias disponíveis.</option>
        {{/if}}
      </select>
    </div>
    <button type="submit" class="btn btn-primary mt-3">Salvar Produto</button>
  </form>
</div>

{{!<div class="form-row">
  <div class="form-group col-md-4">
    <label for="idchave">ID da chave</label>
    <select name="idchave" id="idchave" class="form-control">
    {{#if tb_chave.length}}
      {{!#each tb_chave}}
        {{!<option value={{this.id_chave}}{{!this.titulo</option>}}
      {{!/each}}
      {{!else}}
        {{!<option value="NA">Sem chaves disponíveis.</option>}}
      {{!/if}}
    {{!</select>}}
  {{!</div>}}
{{!</div>}}